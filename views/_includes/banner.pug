mixin banner(variant)
  div(class="banner banner--" + variant +"")
    span(class="banner__icon banner--"+ variant +"__icon" aria-hidden="true")= variant !== 'check' ? 'i' : null
    if block
      block
    else
      p #{__('No content provided')}
