{% macro validationMessage(msg, id) %}
    <span class="validation-message" id="{{ id }}-error">
        <span class="visually-hidden">{{ __('Error:') }}</span>
        {{ __(msg) }}
    </span>
{% endmacro %}