{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('landing-page.title') }}</h1>

    <div>
        <p>
            {{ __('landing-page.intro') }}
        </p>
        <p>
            {{ __('landing-page.steps-lead-in') }}
        </p>
        <ul>
            <li>
                {{ __('landing-page.steps-recruit') }}
            </li>
            <li>
                {{ __('landing-page.steps-tools') }}
            </li>
            <li>
                {{ __('landing-page.steps-activities') }}
            </li>
        </ul>
        <p>
            <a href="{{questionUrls.cds}}">{{ __('landing-page.questionnaires.cds') }}</a>.
        </p>
        <p>
            <a href="{{questionUrls.generic}}">{{ __('landing-page.questionnaires.generic') }}</a>.
        </p>
       

    </div>

{% endblock %}
