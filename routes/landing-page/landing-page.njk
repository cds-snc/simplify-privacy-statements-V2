{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('landing-page.title') }}</h1>

    <div>
        <p>
            {{ __('landing-page.intro') }}
        </p>
        <form method="post">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            {{ radioButtons('template', { '1':__('landing-page.option1'),'2':__('landing-page.option2')}, data.template, __('landing-page.leadin'), errors) }}
            
            {{ submitButton(__('start.submit')) }}
        </form>
    </div>

{% endblock %}
