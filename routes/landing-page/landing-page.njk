{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('landing-page.title') }}</h1>

    <div>
        <p>
            {{ __('landing-page.intro') }}
        </p>
        {# <p>
            {{ __('landing-page.leadin') }}
        </p> #}
        <ul>
            <li>
                <a href="{{questions1URL}}">{{ __('landing-page.option1') }}</a>
            </li>
            {# <li>
                <a href="{{questions2URL}}">{{ __('landing-page.option2') }}</a>
            </li> #}
        </ul>

    </div>

{% endblock %}
